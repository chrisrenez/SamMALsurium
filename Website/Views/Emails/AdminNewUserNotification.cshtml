@model dynamic
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .content {
            margin-bottom: 20px;
        }
        .user-details {
            background-color: #f8f9fa;
            padding: 15px;
            border-left: 4px solid #007bff;
            margin: 20px 0;
        }
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #dee2e6;
            font-size: 0.9em;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>Neue Mitgliederanfrage</h2>
    </div>

    <div class="content">
        <p>Hallo,</p>

        <p>Es gibt eine neue Mitgliederanfrage auf SamMALsurium:</p>

        <div class="user-details">
            <strong>Name:</strong> @Model.NewUserFirstName @Model.NewUserLastName<br>
            <strong>E-Mail:</strong> @Model.NewUserEmail
        </div>

        <p>Bitte melde dich im Admin-Bereich an, um die Anfrage zu bearbeiten.</p>
    </div>

    <div class="footer">
        <p>Diese E-Mail wurde automatisch von SamMALsurium gesendet.</p>
    </div>
</body>
</html>
