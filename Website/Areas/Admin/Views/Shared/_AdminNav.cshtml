@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

<nav class="admin-navigation">
    <div class="container-fluid px-4">
        <div class="container-xxl">
            <div class="d-flex justify-content-center py-3">
                <div class="btn-group" role="group" aria-label="Admin navigation">
                    <a href="/Admin/Dashboard"
                       class="btn btn-admin-nav @(currentController == "Dashboard" ? "active" : "")">
                        <i class="bi bi-speedometer2 me-2"></i>Übersicht
                    </a>
                    <a href="/Admin/Members"
                       class="btn btn-admin-nav @(currentController == "Members" ? "active" : "")">
                        <i class="bi bi-people me-2"></i>Mitglieder
                    </a>
                    <a href="#"
                       class="btn btn-admin-nav disabled"
                       tabindex="-1"
                       aria-disabled="true">
                        <i class="bi bi-calendar-event me-2"></i>Veranstaltungen
                    </a>
                    <a href="#"
                       class="btn btn-admin-nav disabled"
                       tabindex="-1"
                       aria-disabled="true">
                        <i class="bi bi-bar-chart me-2"></i>Umfragen
                    </a>
                </div>
            </div>
        </div>
    </div>
</nav>
