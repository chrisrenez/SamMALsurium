@{
    var userName = ViewBag.UserName ?? "Mitglied";
    var pollTitle = ViewBag.PollTitle ?? "";
    var pollDescription = ViewBag.PollDescription ?? "";
    var eventTitle = ViewBag.EventTitle;
    var resultsUrl = ViewBag.ResultsUrl ?? "";
}
<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #9b59b6;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f9f9f9;
            padding: 30px;
            border-radius: 0 0 5px 5px;
        }
        .poll-title {
            font-size: 1.3em;
            font-weight: bold;
            color: #2c3e50;
            margin: 20px 0 10px 0;
        }
        .poll-description {
            color: #555;
            margin-bottom: 20px;
        }
        .event-badge {
            display: inline-block;
            background-color: #3498db;
            color: white;
            padding: 5px 15px;
            border-radius: 15px;
            font-size: 0.9em;
            margin-bottom: 20px;
        }
        .btn {
            display: inline-block;
            background-color: #9b59b6;
            color: white;
            padding: 12px 30px;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin: 20px 0;
        }
        .footer {
            text-align: center;
            color: #777;
            font-size: 0.9em;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>📊 Ergebnisse verfügbar</h1>
    </div>
    <div class="content">
        <p>Hallo @userName,</p>

        <p>Die Umfrage ist jetzt geschlossen und die Ergebnisse sind verfügbar!</p>

        @if (eventTitle != null)
        {
            <div class="event-badge">
                @eventTitle
            </div>
        }

        <div class="poll-title">@pollTitle</div>

        @if (!string.IsNullOrEmpty(pollDescription))
        {
            <div class="poll-description">@pollDescription</div>
        }

        <p>Schau dir an, wie die Community abgestimmt hat und erfahre das Ergebnis der Umfrage.</p>

        <center>
            <a href="@resultsUrl" class="btn">Ergebnisse ansehen</a>
        </center>

        <div class="footer">
            <p>Diese E-Mail wurde automatisch von SamMALsurium gesendet.</p>
            <p>Du kannst deine Benachrichtigungseinstellungen in deinem Profil anpassen.</p>
        </div>
    </div>
</body>
</html>
